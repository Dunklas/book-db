name: Frontend validation
on: pull_request
jobs:
  frontend-validation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd frontend
      - run: npm run lint
      - run: npm run test
