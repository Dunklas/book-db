name: Frontend validation
on: pull_request
jobs:
  frontend-validation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
        cd frontend
        npm run lint
        npm run test
